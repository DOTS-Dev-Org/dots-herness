Compile `src/HelloPlugin` (`HelloPlugin.dll`) and drop it next to this
`plugin.yml` under `%AppData%\DotsHarness\plugins\hello\`. Trust the plugin
in Settings before it will load — compiled assemblies start untrusted.
