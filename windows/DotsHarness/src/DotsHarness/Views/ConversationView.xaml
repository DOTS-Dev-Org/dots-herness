<UserControl x:Class="DotsHarness.Views.ConversationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:views="clr-namespace:DotsHarness.Views"
             Background="{DynamicResource PanelBackground}">
    <DockPanel>
        <DockPanel DockPanel.Dock="Top" Margin="20,14,20,8">
            <TextBlock x:Name="TitleText" FontSize="16" FontWeight="SemiBold"
                       Foreground="{DynamicResource TextPrimary}" VerticalAlignment="Center"/>
            <Button x:Name="StopButton" Content="Stop" HorizontalAlignment="Right"
                    Padding="12,4" Click="OnStop" Visibility="Collapsed"/>
        </DockPanel>
        <Border DockPanel.Dock="Bottom" Background="{DynamicResource ChromeBackground}" Padding="16">
            <DockPanel>
                <views:SlotHost x:Name="ComposerAccessory" DockPanel.Dock="Top" Margin="0,0,0,8"/>
                <Button DockPanel.Dock="Right" Content="Send" Width="80" Height="36"
                        Margin="8,0,0,0" Click="OnSend" IsDefault="True"/>
                <TextBox x:Name="DraftBox"
                         AcceptsReturn="True"
                         TextWrapping="Wrap"
                         MinHeight="36" MaxHeight="140"
                         VerticalScrollBarVisibility="Auto"
                         Padding="10,8"
                         PreviewKeyDown="OnDraftKey"/>
            </DockPanel>
        </Border>
        <Border x:Name="QuestionBar" DockPanel.Dock="Bottom" Background="#142F6FED" Padding="12" Visibility="Collapsed">
            <DockPanel>
                <TextBlock x:Name="QuestionPrompt" FontWeight="SemiBold" DockPanel.Dock="Top" Margin="0,0,0,8"
                           Foreground="{DynamicResource TextPrimary}" TextWrapping="Wrap"/>
                <WrapPanel x:Name="QuestionOptions"/>
            </DockPanel>
        </Border>
        <Border x:Name="ApprovalBar" DockPanel.Dock="Bottom" Background="#22E67E22" Padding="12" Visibility="Collapsed">
            <DockPanel>
                <StackPanel>
                    <TextBlock x:Name="ApprovalTitle" FontWeight="SemiBold" Foreground="{DynamicResource TextPrimary}"/>
                    <TextBlock x:Name="ApprovalReason" FontSize="11" Foreground="{DynamicResource TextSecondary}"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="Reject" Margin="0,0,8,0" Click="OnReject"/>
                    <Button Content="Allow once" Click="OnAllow"/>
                </StackPanel>
            </DockPanel>
        </Border>
        <ScrollViewer x:Name="Scroller" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <TextBlock x:Name="EmptyHint"
                           Margin="24"
                           FontSize="14"
                           Foreground="{DynamicResource TextSecondary}"
                           Text="Start a conversation from the sidebar."/>
                <ItemsControl x:Name="Messages" Margin="24,8,24,24">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Margin="0,0,0,12" Padding="12" CornerRadius="12"
                                    Background="{Binding Kind, Converter={x:Static views:KindBrush.Instance}}">
                                <StackPanel>
                                    <DockPanel>
                                        <TextBlock Text="{Binding Kind, Converter={x:Static views:KindLabel.Instance}}"
                                                   FontSize="11" FontWeight="SemiBold"
                                                   Foreground="{DynamicResource TextSecondary}"/>
                                        <TextBlock Text="…" HorizontalAlignment="Right"
                                                   Visibility="{Binding Streaming, Converter={x:Static views:BoolToVis.Instance}}"/>
                                    </DockPanel>
                                    <TextBlock Text="{Binding Text}" TextWrapping="Wrap" Margin="0,6,0,0"
                                               Foreground="{DynamicResource TextPrimary}"/>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
                <ItemsControl x:Name="PendingMessages" Margin="24,0,24,24">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Margin="0,0,0,12" Padding="12" CornerRadius="12"
                                    Background="{DynamicResource BubbleUser}" Opacity="0.72">
                                <StackPanel>
                                    <DockPanel>
                                        <TextBlock Text="{Binding Mode, Converter={x:Static views:PromptModeLabel.Instance}}"
                                                   FontSize="11" FontWeight="SemiBold"
                                                   Foreground="{DynamicResource TextSecondary}"/>
                                        <TextBlock Text="{Binding Placement, Converter={x:Static views:PromptPlacementLabel.Instance}}"
                                                   HorizontalAlignment="Right" FontSize="11"
                                                   Foreground="{DynamicResource TextSecondary}"/>
                                    </DockPanel>
                                    <TextBlock Text="{Binding Text}" TextWrapping="Wrap" Margin="0,6,0,0"
                                               Foreground="{DynamicResource TextPrimary}"/>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
        </ScrollViewer>
    </DockPanel>
</UserControl>
